VLCcontrols is a cross browser javascript library to add controls to the VLC plugins

Works on IE/Firefox/Chrome/Safari

now need some rewrite so moved to github !

recent VLC >= 1.0 new security restrictions disable lots of plugin functionnality